{
  "name": "vigilops-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Quickstart fixture for VigilOps — a minimal Node project with a known-vulnerable dependency so users can see `vigilops scan .` produce real reachable output immediately.",
  "dependencies": {
    "lodash": "4.17.15"
  }
}
